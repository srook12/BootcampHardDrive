mkdir /c/ssa is like mkdir C:\ssa
cd /c/ssa

// Identify yourself to git
git config --global user.email "srook12@gmail.com"
git config --global user.name "srook12"

git init 
	Establishes a repository in the current directory. Default branch is master.

git status
	Untracked file means git knows it's there, but is doing nothing with it.

git add <files> 
	Adds untracked files to the staging area



